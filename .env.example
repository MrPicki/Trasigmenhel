# The newsletter form's Web3Forms key already has a committed default in
# src/components/NewsletterForm.tsx (Web3Forms access keys are meant to be
# public/client-side, per their own docs). Only set this if you ever need
# to swap in a different key without touching the source:
VITE_WEB3FORMS_KEY=
